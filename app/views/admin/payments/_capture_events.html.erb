<% if @payment.capture_events.exists? %>
  <h3><%= t(:capture_events) %></h3>
  <table class="table table-condensed table-bordered" id="capture_events">
    <thead>
      <tr data-hook="payments_header">
        <th><%= t(:date_time) %></th>
        <th><%= t(:amount) %></th>
      </tr>
    </thead>
    <tbody>
      <% @payment.capture_events.each do |capture_event| %>
        <tr id="<%= dom_id(capture_event) %>" data-hook="capture_events_row">
          <td><%= pretty_time(capture_event.created_at) %></td>
          <td><%= capture_event.display_amount.to_display %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>